# RabbitMQ 4.3+ disables transient, non-exclusive queue declarations by default
# (see the 4.3.0 release notes). Langohr's test suite intentionally exercises
# this long-standing queue type, so permit it for CI runs.
deprecated_features.permit.transient_nonexcl_queues = true
